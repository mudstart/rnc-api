<%= form_with(model: rnc, local: true) do |form| %>
  <% if rnc.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(rnc.errors.count, "error") %> prohibited this rnc from being saved:</h2>

      <ul>
      <% rnc.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
