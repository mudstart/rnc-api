<p id="notice"><%= notice %></p>

<h1>Rncs</h1>

<table>
  <thead>
    <tr>
      <th>Nombre</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @rncs.each do |rnc| %>
      <tr>
        <td><%= rnc.nombre %></td>
        <td><%= link_to 'Show', rnc %></td>
        <td><%= link_to 'Edit', edit_rnc_path(rnc) %></td>
        <td><%= link_to 'Destroy', rnc, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Rnc', new_rnc_path %>
